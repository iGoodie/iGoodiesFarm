/*
 * Decompiled with CFR 0_102.
 * 
 * Could not load the following classes:
 *  cpw.mods.fml.common.eventhandler.SubscribeEvent
 *  net.minecraft.item.ItemStack
 *  net.minecraft.util.StatCollector
 *  net.minecraftforge.event.entity.player.ItemTooltipEvent
 */
package com.pam.harvestcraft;

import cpw.mods.fml.common.eventhandler.SubscribeEvent;
import java.util.ArrayList;
import java.util.Collection;
import java.util.List;
import net.minecraft.item.ItemStack;
import net.minecraft.util.StatCollector;
import net.minecraftforge.event.entity.player.ItemTooltipEvent;

public class PamTooltipEventHandler {
    @SubscribeEvent
    public void onItemTooltip(ItemTooltipEvent event) {
        String tooltip;
        ArrayList<String> toolTipStringsToAdd = new ArrayList<String>();
        if (event.itemStack != null && (tooltip = PamTooltipEventHandler.localize("tooltip.harvestcraft." + event.itemStack.func_77977_a())) != null) {
            if (tooltip.contains((CharSequence)"\\n")) {
                String[] tooltips = tooltip.split("\\\\n");
                int e = tooltips.length;
                for (int i = 0; i < e; ++i) {
                    toolTipStringsToAdd.add(tooltips[i]);
                }
            } else {
                toolTipStringsToAdd.add(tooltip);
            }
        }
        event.toolTip.addAll(toolTipStringsToAdd);
    }

    public static String localize(String s) {
        if (!StatCollector.func_94522_b((String)s)) {
            return null;
        }
        return StatCollector.func_74838_a((String)s);
    }
}

