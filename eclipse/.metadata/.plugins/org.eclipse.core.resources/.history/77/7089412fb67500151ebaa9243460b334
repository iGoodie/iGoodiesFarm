/*
 * Decompiled with CFR 0_102.
 * 
 * Could not load the following classes:
 *  net.minecraft.block.Block
 *  net.minecraft.item.ItemStack
 *  net.minecraft.world.World
 */
package com.pam.harvestcraft;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.Random;
import net.minecraft.block.Block;
import net.minecraft.item.ItemStack;
import net.minecraft.world.World;
import powercrystals.minefactoryreloaded.api.HarvestType;
import powercrystals.minefactoryreloaded.api.IFactoryHarvestable;

public class HarvestableMFRCrop
implements IFactoryHarvestable {
    private Block sourceId;
    private HarvestType harvestType;

    public HarvestableMFRCrop(Block sourceId, HarvestType harvestType) {
        this.sourceId = sourceId;
        this.harvestType = harvestType;
    }

    @Override
    public Block getPlant() {
        return this.sourceId;
    }

    @Override
    public HarvestType getHarvestType() {
        return this.harvestType;
    }

    @Override
    public boolean breakBlock() {
        return true;
    }

    @Override
    public boolean canBeHarvested(World world, Map<String, Boolean> harvesterSettings, int x, int y, int z) {
        return world.func_72805_g(x, y, z) == 7;
    }

    @Override
    public List<ItemStack> getDrops(World world, Random rand, Map<String, Boolean> harvesterSettings, int x, int y, int z) {
        return this.sourceId.getDrops(world, x, y, z, world.func_72805_g(x, y, z), 0);
    }

    @Override
    public void preHarvest(World world, int x, int y, int z) {
    }

    @Override
    public void postHarvest(World world, int x, int y, int z) {
    }
}

